generator client {
  provider = "prisma-client-js"
}
datasource database {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
model usuarios {
  id          Int     @id @default(autoincrement())
  nombre      String? @database.VarChar(50)
  contrasenia String? @database.VarChar(50)
  edad        Int?
}
